get running

dev
- dev container & EnsureCreate() in debug builds
- production compose & migrations setup (how to initialize?)

fix bugs and polish
deploy to cloudflare vps
    x buy domain
    x flyio vps
    x nginx
    - CD
